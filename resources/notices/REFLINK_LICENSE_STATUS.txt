The optional @reflink/reflink file-copy helper is deliberately excluded from the InkCling release. Its upstream package/repository omits a complete copyright/license file. ipull treats this helper as optional and catches absence before falling back to normal copying. InkCling uses its own HTTP download path and loads its bundled local models directly. No reflink binary is shipped.
